<div class="relative mb-8 w-full border-b border-layer-line pb-6">
    <h1 class="text-3xl font-semibold text-foreground">{{ __('Settings') }}</h1>
    <p class="mt-2 text-sm text-muted-foreground">{{ __('Manage your profile and account settings') }}</p>
</div>
